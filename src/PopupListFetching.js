var React = require('react');

var PopupListFetching = React.createClass({

  render: function() {
    return (
      <div aria-live="polite">Loading...</div>
    );
  }

});

module.exports = PopupListFetching;
